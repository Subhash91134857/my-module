// If there is situation where we have to use the some modules too many times,
// doing every time :: is hectic so use the "use keyword to bring the module into scope"
pub fn sub_mode_hole_again() {
    println!("Sub module for use keyword");
}
